# Cloudflare Pages + Workers 설정
name = "elspa-frontend"
type = "javascript"

# 환경변수 (Cloudflare Pages 대시보드에서 설정해주세요)
[env.production]
vars = { NEXT_PUBLIC_API_URL = "https://elspa.pages.dev", NEXT_PUBLIC_USE_MOCK_API = "true" }

# 빌드 설정
[build]
command = "npm run build"
cwd = "."
