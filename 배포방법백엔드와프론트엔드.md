# ElSpa 배포 가이드 (Backend + Frontend)

> ElSpa 프로젝트의 완전한 배포 방법을 단계별로 정리했습니다.
> 
> **배포 대상:**
> - Backend: Railway.app (FastAPI)
> - Frontend: Cloudflare Pages (Next.js)
> - Database: Supabase PostgreSQL

---

## 📋 배포 아키텍처

```
┌─────────────────────────────────────────┐
│ Browser                                 │
│ https://elspa.pages.dev                │
└────────────────┬────────────────────────┘
                 │
┌────────────────▼────────────────────────┐
│ Cloudflare Pages (Frontend)             │
│ • React/Next.js 16.2.4                  │
│ • Monitor (BOOKING WITH THERAPIST)      │
│ • Admin (Settings, Massage Types, etc)  │
└────────────────┬────────────────────────┘
                 │ API 요청
┌────────────────▼────────────────────────────────┐
│ Railway.app (Backend)                          │
│ • FastAPI (Python)                             │
│ • 6개 API 엔드포인트 (/admin/massage-types)    │
│ • 헬스 체크 (/health)                          │
└────────────────┬────────────────────────────────┘
                 │
┌────────────────▼────────────────────────┐
│ Supabase PostgreSQL                     │
│ • massage_services 테이블               │
│ • 초기 데이터 (6개 기본 마사지 종류)     │
└─────────────────────────────────────────┘
```

---

## 🔧 배포 3단계

### **1️⃣ Supabase 데이터베이스 마이그레이션**

#### 목표
- `massage_services` 테이블 생성
- 초기 데이터 (6개 마사지 종류) 추가
- RLS 정책 설정

#### 절차

**1. Supabase 대시보드 접속**
- 🔗 https://app.supabase.com
- 프로젝트 선택

**2. SQL Editor 열기**
- 왼쪽 메뉴 → **SQL Editor**
- **New Query** 클릭

**3. 다음 SQL 실행**

```sql
-- ============================================================
-- massage_services 테이블 생성
-- ============================================================

CREATE TABLE IF NOT EXISTS public.massage_services (
  id bigint generated always as identity primary key,
  name text not null unique,
  base_price numeric(10,2) not null check (base_price >= 0),
  base_duration_minutes integer not null check (base_duration_minutes > 0),
  description text,
  is_active boolean not null default true,
  icon text,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

-- 인덱스 생성
CREATE INDEX IF NOT EXISTS idx_massage_services_active ON public.massage_services(is_active);
CREATE INDEX IF NOT EXISTS idx_massage_services_name ON public.massage_services(name);

-- updated_at 자동 업데이트 트리거
DROP TRIGGER IF EXISTS trg_massage_services_touch ON public.massage_services;

CREATE OR REPLACE FUNCTION public.touch_updated_at()
RETURNS TRIGGER LANGUAGE PLPGSQL AS $$
BEGIN
  new.updated_at = now();
  RETURN new;
END $$;

CREATE TRIGGER trg_massage_services_touch BEFORE UPDATE ON public.massage_services
  FOR EACH ROW EXECUTE FUNCTION public.touch_updated_at();

-- Row Level Security (RLS) 활성화
ALTER TABLE public.massage_services ENABLE ROW LEVEL SECURITY;
DROP POLICY IF EXISTS anon_all_massage_services ON public.massage_services;
CREATE POLICY anon_all_massage_services ON public.massage_services
  FOR ALL TO anon USING (true) WITH CHECK (true);

-- 초기 데이터 삽입 (6개 마사지 종류)
INSERT INTO public.massage_services (name, base_price, base_duration_minutes, description, icon) VALUES
  ('Swedish Massage', 1500, 60, '근육의 긴장을 풀어주는 스웨디시 마사지', '💆'),
  ('Thai Massage', 1500, 90, '전통 타이 마사지로 깊은 휴식 제공', '🙏'),
  ('Hot Stone Massage', 1800, 60, '따뜻한 돌로 근육을 이완시키는 테라피', '🪨'),
  ('Deep Tissue', 2000, 60, '깊은 근층을 집중적으로 치료하는 마사지', '💪'),
  ('Aromatherapy', 1200, 45, '향기 치료와 함께하는 휴식 마사지', '🌸'),
  ('Facial Treatment', 1000, 45, '얼굴 피부 관리 및 스킨케어', '✨')
ON CONFLICT (name) DO NOTHING;
```

**4. ✅ 확인**
```
$ SELECT COUNT(*) FROM public.massage_services;
 count 
-------
     6
```

---

### **2️⃣ Railway 백엔드 배포**

#### 목표
- FastAPI 서버를 Railway에 배포
- Supabase PostgreSQL 연동
- 6개 마사지 타입 API 엔드포인트 활성화

#### 절차

**1. Railway 대시보드 접속**
- 🔗 https://railway.app
- GitHub 로그인

**2. 새 프로젝트 생성**
- **+ New Project** 버튼
- **Deploy from GitHub** 선택

**3. GitHub 저장소 선택**
```
Repository: jitnet57/elspa
Branch: main
```

**4. 배포 설정**

| 설정 항목 | 값 |
|---------|-----|
| **Root Directory** | `./` (또는 비움) |
| **Build Command** | `pip install -r requirements.txt` |
| **Start Command** | `uvicorn main:app --host 0.0.0.0 --port $PORT` |

**5. 환경 변수 설정**

| 변수명 | 값 |
|-------|-----|
| `DATABASE_URL` | Supabase PostgreSQL 연결 문자열 |
| `PORT` | `8000` |

> **DATABASE_URL 찾기:**
> - Supabase Dashboard → Settings → Database
> - Connection Strings → URI (PostgreSQL)
> - 예: `postgresql://user:password@db.supabase.co:5432/postgres`

**6. Deploy 클릭**

배포 진행 (약 2-3분)

**7. ✅ 배포 URL 확인**

Deployments → 최신 항목 → **URL** 섹션
```
https://elspa-api-xxxxx.up.railway.app
```

**8. 헬스 체크**

```bash
# 터미널에서
curl https://elspa-api-xxxxx.up.railway.app/health

# 응답 예:
# {"status":"ok","environment":"production","message":"ElSpa Backend is running!"}
```

**9. API 엔드포인트 확인**

```bash
# 마사지 종류 조회
curl https://elspa-api-xxxxx.up.railway.app/admin/massage-types

# 응답 예:
# {
#   "data": [
#     {"id": 1, "name": "Swedish Massage", "base_price": 1500, ...},
#     ...
#   ],
#   "total_count": 6,
#   "active_count": 6
# }
```

---

### **3️⃣ Cloudflare Pages 프론트엔드 배포 + 환경 변수 설정**

#### 목표
- Next.js 프론트엔드를 Cloudflare Pages에 배포
- Backend API URL 연동

#### 절차

**1. Cloudflare 대시보드 접속**
- 🔗 https://dash.cloudflare.com
- GitHub 로그인

**2. Pages 프로젝트 확인**
- 왼쪽 메뉴 → **Pages**
- 기존 "elspa" 프로젝트 있음 (이미 GitHub 연결됨)

**3. 환경 변수 설정**

**elspa 프로젝트 선택** → **Settings** → **Environment Variables**

| 환경 | 변수명 | 값 |
|-----|-------|-----|
| Production | `NEXT_PUBLIC_API_URL` | `https://elspa-api-xxxxx.up.railway.app` |
| Preview | `NEXT_PUBLIC_API_URL` | `https://elspa-api-xxxxx.up.railway.app` |

> Railway URL 복사해서 붙여넣기 (위에서 확인한 URL)

**4. Save and Deploy**

변경사항 저장 시 자동 재배포 시작 (약 1-2분)

**5. ✅ Frontend 접속 확인**

```
https://elspa.pages.dev
```

### 배포 후 기능 확인

#### Landing Page (홈)
```
https://elspa.pages.dev/
→ Monitor / Admin 선택 메뉴 표시
```

#### Monitor (BOOKING WITH THERAPIST)
```
https://elspa.pages.dev/monitor
→ 예약 테이블 표시
→ 오른쪽으로 스크롤하면 "💳 결제 수단" 버튼
→ 클릭하면 다중결제 모달 열림
```

#### Admin Dashboard
```
https://elspa.pages.dev/admin
→ Dashboard 탭 (기본)
→ 왼쪽 사이드바 메뉴 전체 표시
```

#### 마사지 종류 관리 (NEW!)
```
https://elspa.pages.dev/admin/massage
→ "💆 마사지 종류" 탭 클릭
→ 마사지 종류 추가/수정/삭제
→ 자동으로 Supabase에 저장
→ API 통해 Monitor 페이지 서비스 선택에 반영
```

---

## 🧪 배포 후 완전한 테스트

### 1️⃣ Backend API 테스트

```bash
# 헬스 체크
curl https://elspa-api-xxxxx.up.railway.app/health

# 모든 마사지 종류 조회
curl https://elspa-api-xxxxx.up.railway.app/admin/massage-types

# 특정 마사지 종류 조회 (ID=1)
curl https://elspa-api-xxxxx.up.railway.app/admin/massage-types/1

# 새 마사지 종류 생성 (POST)
curl -X POST https://elspa-api-xxxxx.up.railway.app/admin/massage-types \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Shiatsu Massage",
    "base_price": 1600,
    "base_duration_minutes": 60,
    "description": "지압을 이용한 일본 마사지",
    "icon": "🤲"
  }'
```

### 2️⃣ Frontend UI 테스트

#### Landing Page
- [ ] Monitor 버튼 클릭 → `/monitor` 이동 확인
- [ ] Admin 버튼 클릭 → `/admin` 이동 확인

#### Monitor Page
- [ ] 예약 테이블 로드 확인
- [ ] 💳 결제 수단 버튼 클릭 가능
- [ ] 다중결제 모달 열림
- [ ] 결제 수단 추가/삭제 가능

#### Admin Page
- [ ] 왼쪽 사이드바 메뉴 전체 표시 확인
- [ ] Dashboard/Payroll/Settings 등 탭 이동 가능

#### 마사지 종류 관리
- [ ] `/admin/massage` → "💆 마사지 종류" 탭
- [ ] 기존 6개 마사지 종류 표시
- [ ] "마사지 종류 추가" 버튼 클릭
- [ ] 새 항목 입력 후 저장
- [ ] Supabase에 자동 저장 확인
- [ ] Monitor 페이지 서비스 dropdown에 반영 확인

---

## 📊 배포 확인 체크리스트

### Supabase
- [ ] massage_services 테이블 생성됨
- [ ] 6개 초기 데이터 삽입됨
- [ ] RLS 정책 활성화됨

### Railway Backend
- [ ] 배포 완료 (green status)
- [ ] `/health` 엔드포인트 응답 정상
- [ ] `/admin/massage-types` 6개 데이터 반환
- [ ] DATABASE_URL 환경 변수 설정됨

### Cloudflare Pages Frontend
- [ ] 자동 배포 완료
- [ ] `NEXT_PUBLIC_API_URL` 환경 변수 설정됨
- [ ] https://elspa.pages.dev 접속 가능
- [ ] Landing Page → Monitor/Admin 이동 가능
- [ ] Admin Massage 페이지에서 API 데이터 로드 확인

---

## 🔗 유용한 링크

| 서비스 | URL |
|-------|-----|
| **Supabase** | https://app.supabase.com |
| **Railway** | https://railway.app |
| **Cloudflare** | https://dash.cloudflare.com |
| **GitHub** | https://github.com/jitnet57/elspa |
| **Frontend** | https://elspa.pages.dev |
| **Backend API** | https://elspa-api-xxxxx.up.railway.app |

---

## ⚠️ 트러블슈팅

### Frontend에서 API 연결 안 됨
- [ ] `NEXT_PUBLIC_API_URL` 환경 변수 설정 확인
- [ ] Railway URL 복사 정확성 확인
- [ ] 개발자 도구 Console에서 에러 메시지 확인

### Railway 배포 실패
- [ ] `requirements.txt` 파일 존재 확인
- [ ] DATABASE_URL 형식 확인 (postgresql://...)
- [ ] Build/Start 커맨드 정확성 확인

### Supabase 테이블 생성 실패
- [ ] SQL 문법 오류 확인
- [ ] 테이블명 (massage_services) 중복 확인
- [ ] RLS 정책 설정 확인

---

## 📝 배포 기록

| 날짜 | 단계 | 상태 | 비고 |
|------|------|------|------|
| 2026-06-03 | Supabase Migration | ✅ | massage_services 테이블 생성 |
| 2026-06-03 | Railway Backend | ⏳ | 진행 중 |
| 2026-06-03 | Cloudflare Frontend | ⏳ | 환경 변수 설정 대기 |

---

## 💡 참고사항

1. **마사지 종류는 동적으로 관리됨**
   - Admin > Massage > "마사지 종류" 탭에서 추가/수정/삭제 가능
   - Monitor 페이지의 서비스 선택이 자동으로 업데이트됨

2. **다중결제 기능 지원**
   - Monitor 페이지에서 💳 결제 수단 버튼으로 여러 결제 방식 분배 가능
   - 지원: 카드, 현금, GCash, 은행A, 은행B

3. **데이터베이스 자동 저장**
   - Admin 설정에서 마사지 종류, 비용, 급여 등이 자동으로 Supabase에 저장됨
   - Google Drive 자동 저장도 별도 설정 가능

4. **환경 변수는 자동 재배포 트리거**
   - Cloudflare Pages의 환경 변수 변경 시 자동으로 재배포됨

---

**작성일:** 2026-06-03  
**최종 업데이트:** 2026-06-03  
**담당자:** jitnet57 (Kang Jichul)
