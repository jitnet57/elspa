# 📕 ELSPA 히스토리북

> 작업 히스토리(이미지 포함). 화면 스크린샷은 `docs/images/` 에 저장 후 참조합니다.
> 형식은 [CLAUDE.md](CLAUDE.md)의 Order 기반 기록 규칙을 따릅니다 (append-only).

---

## [2026-05-31] Order: 100 — 백엔드 제거 + Supabase 직접 연동 + 1시간 Sheet 백업

**주제:** 모니터를 백엔드 없이 Supabase로 직접 구동하고, 변경을 즉시 저장 + 1시간 단위로 Google Sheet에 백업

### Plan
- ✅ GitHub에 노출된 시크릿 제거
- ✅ Supabase 직접 연동 (백엔드/WebSocket 불필요)
- ✅ Monitor 변경 즉시 Supabase 저장, 실패 시 파일(스냅샷) 폴백
- ✅ 1시간 단위 Supabase → Google Sheet 백업 (Apps Script)
- ✅ Booking 30개 단위 페이지(1st/2nd/3rd) + 등록/수정/조회
- ✅ Daily Schedule ⇄ Booking 테이블 뷰 전환
- ✅ 빌드 → 커밋 → 푸시 → (자동)배포 → 랜딩 링크

### 화면 (Screens)

#### 1) Real-time Bed Mode (실시간 침대 모니터)
비어있음/서비스중/예약됨/정리중 상태별 색상, 마사지룸1·2 침대 그리드, 테라피스트 현황 패널.

![실시간 침대 모니터](docs/images/01-monitor-bed-mode.png)

#### 2) Daily Therapist Schedule (Gantt)
날짜 선택 → 테라피스트별 시간대(09:00~21:00) 예약 블록. `📋 Booking Table` 토글로 예약표 전환, `+ Start New Massage` 버튼.

![테라피스트 일일 스케줄](docs/images/02-daily-schedule.png)

#### 3) BOOKING WITH THERAPIST (예약 테이블)
분홍 헤더 + 다크 테이블. DATE/N#/TRT/START/END/RM#/GUEST/NOTE/PAY/TIP/ACTION/STATUS. 행별 수정/삭제, 30개 초과 시 1st/2nd/3rd 페이지.

![예약 테이블](docs/images/03-booking-table.png)

#### 4) (해결 전) WebSocket 무한 재연결 오류
`ws://localhost:8000/ws/monitor` 재연결 실패 → **WebSocket 제거, 5초 폴링 전환**으로 해소.

![WebSocket 오류(해결됨)](docs/images/04-websocket-error-before.png)

### Task 수행 내용

#### 섹션 1: 보안 (시크릿 제거)
1. `.gitignore` — `.env.*` 전체 무시(+`.env.example` 유지)
2. `git rm --cached` — `.env`, `.env.development/production/staging`, `backend.log`
3. `DEPLOYMENT_GUIDE.md`, `CONVERSATION_2026-05-06.md` — 실제 키 마스킹

#### 섹션 2: 데이터 레이어 (백엔드 없음)
1. `frontend/src/lib/supabase/client.ts` — Supabase 싱글톤(anon)
2. `frontend/src/lib/api/supabase-adapter.ts` — beds/therapists/bookings 조회·쓰기 + 스냅샷 폴백
3. `frontend/src/lib/api/get-client.ts` — Mock ⇄ Supabase 전환 seam
4. `supabase/schema.sql` — 테이블 + RLS + 침대 86개 시드
5. `apps-script/Code.gs` — 매시간 Supabase→Sheet 트리거

#### 섹션 3: 프론트엔드 UI
1. `hooks/useBookings.ts` — CRUD + 30개 페이지(1st/2nd/3rd)
2. `hooks/useUpdateBed.ts` — 침대 변경 즉시 저장
3. `monitor/components/BookingModal.tsx` — Supabase 연동 + 페이지 라벨
4. `monitor/components/TherapistScheduleView.tsx` — Gantt ⇄ 테이블 토글
5. `monitor/page.tsx` — WebSocket 제거, 폴링 표시

### Result
✅ **15+개 파일 추가/수정, 3개 커밋, GitHub main 푸시 완료**
- 보안: 시크릿 추적 해제 + 문서 마스킹 ✓
- 백엔드 없는 Supabase 직접 연동 ✓
- 변경 즉시 저장 + 1시간 Sheet 백업 ✓
- Booking 30개 페이지네이션 + CRUD ✓
- Daily Schedule ⇄ Booking 전환 ✓
- WebSocket 제거(폴링) ✓
- 빌드(정적 export) 성공 → `frontend/out/` ✓
- 랜딩(`index.html`) → `/monitor.html` 링크 연결 확인 ✓

### Next
- Cloudflare Pages 자동 배포 결과 확인 (`https://elspa.pages.dev`)
- Supabase 프로젝트에 `schema.sql` 적용 + 프론트 env 설정
- Apps Script 트리거 설치(`setupHourlyTrigger`)
- ⚠️ 노출됐던 키/토큰 재발급

**주요 파일:** supabase-adapter.ts, get-client.ts, useBookings.ts, BookingModal.tsx, TherapistScheduleView.tsx, schema.sql, Code.gs

---

> 📸 이미지 추가 방법: 위 4개 스크린샷을 아래 파일명으로 `docs/images/`에 저장하면 자동 표시됩니다.
> - `01-monitor-bed-mode.png` (실시간 침대 모니터)
> - `02-daily-schedule.png` (테라피스트 일일 스케줄)
> - `03-booking-table.png` (BOOKING WITH THERAPIST)
> - `04-websocket-error-before.png` (해결 전 WebSocket 오류 콘솔)
