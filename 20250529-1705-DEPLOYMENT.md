# ElSpa - 프로덕션 배포 가이드

> 📌 Order 053 - Agent J: 프로덕션 배포 & 성능 최적화 & PWA
> 📅 작성일: 2026-05-29

---

## 목차

1. [배포 아키텍처](#배포-아키텍처)
2. [프론트엔드 배포](#프론트엔드-배포)
3. [백엔드 배포](#백엔드-배포)
4. [성능 측정](#성능-측정)
5. [PWA 검증](#pwa-검증)
6. [모니터링 & 로깅](#모니터링--로깅)
7. [트러블슈팅](#트러블슈팅)
8. [롤백 절차](#롤백-절차)

---

## 배포 아키텍처

### 환경별 배포 계획

```
┌─────────────────────────────────────────────────────────┐
│                    DNS & CDN                             │
│              (Cloudflare, Route 53)                      │
└────────────────────┬────────────────────────────────────┘
                     │
        ┌────────────┼────────────┐
        ▼            ▼            ▼
   ┌─────────┐  ┌─────────┐  ┌─────────┐
   │  Dev    │  │ Staging │  │  Prod   │
   │ Local   │  │ Preview │  │ Live    │
   │ 3000    │  │ Vercel  │  │ Vercel  │
   └─────────┘  └─────────┘  └─────────┘
        │            │            │
        └────────────┼────────────┘
                     ▼
              ┌──────────────┐
              │  FastAPI     │
              │  Backend     │
              │  (Railway)   │
              └──────────────┘
                     │
                     ▼
              ┌──────────────┐
              │ PostgreSQL   │
              │ (Supabase)   │
              └──────────────┘
```

---

## 프론트엔드 배포

### Step 1: 로컬 프로덕션 빌드 테스트

```bash
cd /e/elspa/frontend

# 프로덕션 빌드 (정적 생성)
npm run build

# 번들 크기 확인
du -sh out/
# 결과: ~12MB (Three.js, 이미지 포함)

# 로컬에서 프로덕션 서빙 테스트
npm run start
# http://localhost:3000에서 확인

# Lighthouse 성능 측정
# - Chrome DevTools > Lighthouse > Analyze page load
# - 목표: LCP < 2.5s, CLS < 0.1, FID < 100ms
```

### Step 2: 환경 변수 설정

**파일: `.env.production`**

```env
# 📌 API 설정
NEXT_PUBLIC_API_URL=https://api.elspa.com
NEXT_PUBLIC_ANALYTICS_ENDPOINT=https://analytics.elspa.com/metrics

# 📌 Third-party 서비스
NEXT_PUBLIC_GOOGLE_MAPS_KEY=<Google Maps API Key>
NEXT_PUBLIC_SENTRY_DSN=<Sentry DSN for error tracking>

# 📌 기능 플래그
NEXT_PUBLIC_ENABLE_3D_NETWORK=true
NEXT_PUBLIC_ENABLE_OFFLINE_MODE=true
NEXT_PUBLIC_ENABLE_PWA=true

# 📌 성능
NEXT_PUBLIC_PERFORMANCE_THRESHOLD_LCP=2500
NEXT_PUBLIC_PERFORMANCE_THRESHOLD_FID=100
```

### Step 3: Vercel에 배포

#### 3-1. GitHub 연동

```bash
# Vercel CLI 설치
npm install -g vercel

# Vercel 로그인
vercel login

# 프로젝트 연동
cd /e/elspa/frontend
vercel link --project=elspa-frontend
```

#### 3-2. 자동 배포 설정

**파일: `.github/workflows/deploy-frontend.yml`**

```yaml
name: Deploy Frontend (Vercel)

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run build
        run: npm run build

      - name: Run tests
        run: npm run test

      - name: Deploy to Vercel
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        run: vercel deploy --prod --token=$VERCEL_TOKEN
```

#### 3-3. 성능 최적화 설정 (Vercel)

Vercel 대시보드:
- **Edge Functions**: API 라우트 엣지 실행
- **Image Optimization**: 자동 이미지 최적화 (활성화)
- **Gzip Compression**: 활성화
- **Cache-Control Headers**: 설정 (아래 참조)

### Step 4: 캐시 헤더 설정

**파일: `vercel.json`**

```json
{
  "headers": [
    {
      "source": "/static/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    },
    {
      "source": "/_next/static/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    },
    {
      "source": "/",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=0, must-revalidate"
        },
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        },
        {
          "key": "X-XSS-Protection",
          "value": "1; mode=block"
        },
        {
          "key": "Strict-Transport-Security",
          "value": "max-age=31536000; includeSubDomains; preload"
        },
        {
          "key": "Content-Security-Policy",
          "value": "default-src 'self'; script-src 'self' 'unsafe-inline' cdn.jsdelivr.net cdnjs.cloudflare.com; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; font-src 'self' data:;"
        }
      ]
    }
  ],
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://api.elspa.com/$1"
    }
  ]
}
```

---

## 백엔드 배포

### Step 1: FastAPI 프로덕션 설정

**파일: `main.py` (수정)**

```python
# ============================================================
# 📌 프로덕션 설정
# ============================================================
import os
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from fastapi.middleware.gzip import GZipMiddleware

app = FastAPI(
    title="ElSpa API",
    version="1.0.0",
    docs_url="/docs" if os.getenv("ENV") != "production" else None,
    redoc_url=None,
)

# CORS 설정 (프로덕션)
allowed_origins = os.getenv("ALLOWED_ORIGINS", "http://localhost:3000").split(",")
app.add_middleware(
    CORSMiddleware,
    allow_origins=allowed_origins,
    allow_credentials=True,
    allow_methods=["GET", "POST", "PUT", "DELETE"],
    allow_headers=["*"],
)

# Gzip 압축
app.add_middleware(GZipMiddleware, minimum_size=1000)

# 상태 확인 엔드포인트
@app.get("/health")
async def health_check():
    return {
        "status": "healthy",
        "version": "1.0.0",
        "timestamp": datetime.now().isoformat(),
    }
```

### Step 2: Gunicorn + Uvicorn 설정

**파일: `gunicorn_config.py`**

```python
# ============================================================
# 📌 Gunicorn 설정 (프로덕션 ASGI 서버)
# ============================================================
import multiprocessing
import os

# 워커 설정 (CPU 코어 기반)
workers = int(os.getenv("WEB_CONCURRENCY", multiprocessing.cpu_count() * 2 + 1))
worker_class = "uvicorn.workers.UvicornWorker"
worker_connections = 1000
max_requests = 1000
max_requests_jitter = 50

# 바인드 설정
bind = f"0.0.0.0:{os.getenv('PORT', 8000)}"
backlog = 2048

# 타임아웃
timeout = 60
graceful_timeout = 30
keepalive = 5

# 로깅
loglevel = os.getenv("LOG_LEVEL", "info")
access_log_format = '%(h)s %(l)s %(u)s %(t)s "%(r)s" %(s)s %(b)s "%(f)s" "%(a)s" %(D)s'

# 프로세스 명명
proc_name = "elspa-api"

# 예외 처리
preload_app = False
daemon = False
```

### Step 3: Railway에 배포

```bash
# Railway CLI 설치
npm install -g @railway/cli

# Railway 로그인
railway login

# 프로젝트 연동
railway init

# Procfile 생성
cat > Procfile << EOF
web: gunicorn -c gunicorn_config.py main:app
EOF

# 배포
railway up
```

### Step 4: 환경 변수 설정 (Railway)

Railway 대시보드에서 다음 환경 변수 설정:

```
DATABASE_URL=postgresql://user:password@host:5432/elspa
REDIS_URL=redis://host:6379
SECRET_KEY=<생성된 비밀키>
ENVIRONMENT=production
LOG_LEVEL=info
WEB_CONCURRENCY=4
CORS_ORIGINS=https://elspa.com,https://www.elspa.com
```

---

## 성능 측정

### Web Vitals 모니터링

**파일: `src/app/layout.tsx`에 추가**

```typescript
"use client";

import { useEffect } from "react";
import { getPerformanceMonitor } from "@/lib/monitoring/20250529-1700-performance-metrics";

export default function RootLayout({ children }: { children: React.ReactNode }) {
  useEffect(() => {
    // 성능 모니터링 시작
    const monitor = getPerformanceMonitor();

    // 페이지 언로드 시 메트릭 전송
    const handleBeforeUnload = () => {
      monitor.sendToAnalytics(
        process.env.NEXT_PUBLIC_ANALYTICS_ENDPOINT || "/api/metrics"
      );
    };

    window.addEventListener("beforeunload", handleBeforeUnload);

    return () => {
      window.removeEventListener("beforeunload", handleBeforeUnload);
    };
  }, []);

  return <html>{/* ... */}</html>;
}
```

### 목표 성능 지표

| 지표 | 목표 | 현황 | 상태 |
|------|------|------|------|
| LCP | < 2.5s | 측정 중 | - |
| FID | < 100ms | 측정 중 | - |
| CLS | < 0.1 | 측정 중 | - |
| FCP | < 1.8s | 측정 중 | - |
| TTFB | < 600ms | 측정 중 | - |
| 번들 크기 | < 2MB | 12MB | ⚠️ |

---

## PWA 검증

### PWA 체크리스트

```bash
# 1️⃣ Web App Manifest 검증
curl -s https://elspa.com/manifest.json | jq .

# 2️⃣ HTTPS 확인
curl -I https://elspa.com
# HTTP/2 200 (또는 HTTPS 리다이렉트)

# 3️⃣ Service Worker 설치
# Chrome DevTools > Application > Service Workers
# 상태: activated and running

# 4️⃣ 설치 배너 트리거
# - 방문: https://elspa.com
# - 주소 표시줄 설치 버튼 클릭
# - 또는 메뉴 > 앱 설치

# 5️⃣ Lighthouse PWA 점수
# Chrome DevTools > Lighthouse > PWA
# 목표: 90점 이상
```

### PWA 기능 확인

**설치:**
- Chrome 주소 표시줄의 "설치" 버튼 클릭
- 또는 메뉴 > 앱 설치

**오프라인 사용:**
```bash
# 1. 앱 설치 후 실행
# 2. Chrome DevTools > Network > Offline 체크
# 3. 페이지 새로고침
# 결과: 오프라인 페이지 표시 (캐시된 콘텐츠)
```

**정보 패널:**
```bash
# Chrome DevTools > Application > Manifest
# - name: El Plaza - Spa & Wellness Booking Platform
# - display: standalone
# - icons: 192x192, 512x512 (maskable 포함)
# - categories: health, wellness, productivity
```

---

## 모니터링 & 로깅

### 에러 추적 (Sentry)

**파일: `src/lib/monitoring/sentry.ts`**

```typescript
import * as Sentry from "@sentry/nextjs";

export function initSentry() {
  Sentry.init({
    dsn: process.env.NEXT_PUBLIC_SENTRY_DSN,
    environment: process.env.NEXT_ENV || "development",
    tracesSampleRate: process.env.NEXT_ENV === "production" ? 0.1 : 1.0,
    integrations: [
      new Sentry.Replay({
        maskAllText: true,
        blockAllMedia: true,
      }),
    ],
    replaysSessionSampleRate: 0.1,
    replaysOnErrorSampleRate: 1.0,
  });
}
```

### 로깅 모니터링

**로그 수준:**

```
DEBUG (개발)    → 상세 정보 (함수 호출, 변수값 등)
INFO (프로덕션) → 일반 정보 (API 요청, 성공 메시지)
WARNING         → 경고 (deprecated API, 성능 저하)
ERROR           → 오류 (예외, 실패한 요청)
CRITICAL        → 심각 (데이터베이스 다운, 인증 실패)
```

### API 응답 모니터링

**메트릭 대시보드:**

```
/api/metrics
└── 요청 수
    ├── 성공률: 99.5%
    ├── 평균 응답 시간: 145ms
    └── 에러율: 0.5%
```

---

## 트러블슈팅

### 1️⃣ 빌드 에러

```bash
# 캐시 삭제
rm -rf .next node_modules package-lock.json

# 재설치 및 빌드
npm install
npm run build

# TypeScript 에러 확인
npm run build -- --debug
```

### 2️⃣ 배포 실패

```bash
# Vercel 배포 로그 확인
vercel logs

# Railway 배포 로그 확인
railway logs

# 환경 변수 확인
vercel env ls
railway env list
```

### 3️⃣ 성능 저하

```bash
# 1. 번들 분석
npm run build -- --analyze

# 2. 캐시 정책 확인
curl -I https://elspa.com/_next/static/...

# 3. CDN 캐시 제거 (Cloudflare)
wrangler cache purge --url=https://elspa.com
```

### 4️⃣ PWA 설치 실패

```bash
# Chrome DevTools > Application > Manifest
# - 필수: name, icons, start_url, display
# - HTTPS 확인
# - Service Worker 활성화 확인
```

---

## 롤백 절차

### 이전 버전으로 복원

**Vercel:**

```bash
# 배포 목록 확인
vercel ls

# 이전 배포로 복원
vercel promote <deployment-url>
```

**Railway:**

```bash
# 배포 목록 확인
railway status

# 이전 배포로 복원
railway deploy --from-image=<previous-image>
```

### Git을 이용한 롤백

```bash
# 이전 커밋으로 복원
git revert <commit-hash>
git push origin main

# 또는 특정 브랜치에서 배포
git checkout <stable-branch>
git push origin <stable-branch>
```

---

## 성능 최적화 팁

### 1️⃣ 이미지 최적화

```bash
# WebP 변환 (필요 시)
# 자동으로 Cloudflare/Vercel에서 처리
# 수동: cwebp input.png -o output.webp
```

### 2️⃣ 코드 분할 (Dynamic Imports)

```typescript
// Three.js 동적 로드
const KnowledgeNetwork = dynamic(
  () => import("@/components/KnowledgeNetwork"),
  { ssr: false, loading: () => <Skeleton /> }
);
```

### 3️⃣ 번들 크기 감소

```bash
# 사용하지 않는 라이브러리 제거
npm uninstall <package>

# Tree-shaking 확인
npm run build -- --analyze
```

---

## 배포 체크리스트

- [ ] 로컬 프로덕션 빌드 테스트 (`npm run build`)
- [ ] TypeScript 타입 검사 통과 (`npm run build`)
- [ ] Lighthouse 성능 점수 > 90
- [ ] PWA 설치 테스트
- [ ] 환경 변수 설정 확인
- [ ] CORS 도메인 설정
- [ ] SSL/TLS 인증서 확인 (HTTPS)
- [ ] 데이터베이스 마이그레이션 완료
- [ ] API 헬스 체크 (`/health`)
- [ ] 모니터링 & 로깅 활성화
- [ ] 에러 추적 (Sentry) 연동
- [ ] 성능 메트릭 수집 설정
- [ ] 캐시 정책 적용
- [ ] CDN 설정 (Cloudflare)
- [ ] DNS 설정 (elspa.com → CDN)

---

## 참고 자료

- [Next.js 프로덕션 배포](https://nextjs.org/docs/deployment)
- [Vercel 배포 가이드](https://vercel.com/docs)
- [FastAPI 프로덕션](https://fastapi.tiangolo.com/deployment/)
- [Web Vitals](https://web.dev/vitals/)
- [PWA 체크리스트](https://web.dev/pwa-checklist/)
- [Sentry 오류 추적](https://sentry.io/docs/)

---

**작성:** jitnet57 (kang jichul)  
**최종 업데이트:** 2026-05-29 17:05 KST  
**문서 버전:** 1.0
